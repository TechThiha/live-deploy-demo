{
  "name": "live-deploy-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Pages site (static page + Functions + KV) serving try.thiha.cloud — the live 'deploy to production' widget on thiha.cloud.",
  "scripts": {
    "dev": "wrangler pages dev",
    "deploy": "wrangler pages deploy",
    "tail": "wrangler pages deployment tail"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
